# Kärnberoenden. Lösa nedre gränser så att pip väljer en installerbar version
# i den miljö appen körs (verifiera med `pip install -r requirements.txt`).
flask>=3.0
yfinance>=0.2.40
pandas>=2.1
numpy>=1.26
requests>=2.31
gunicorn>=21.2
APScheduler>=3.10,<4
